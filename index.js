// Este arquivo (index.js) é o ponto de entrada padrão para aplicações Node.JS.

// Aqui apenas estamos fazendo a importação do nosso servidor express (server.js).
const server = require('./src/server');
